{
  "name": "hulog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "静态博客生成器——Hexo 内容模型 + Astro 式 TSX 主题",
  "scripts": {
    "build": "npm run build:core && npm run build:cli",
    "build:core": "npm run build --workspace=core",
    "build:cli": "npm run build --workspace=cli",
    "typecheck": "npm run core:typecheck && npm run cli:typecheck",
    "typecheck:core": "npm run typecheck --workspace=core",
    "typecheck:cli": "npm run typecheck --workspace=cli",
    "clean": "npm run core:clean && npm run cli:clean && node -e \"fs.rmSync('node_modules/.cache', { recursive: true, force: true })\"",
    "clean:core": "npm run clean --workspace=core",
    "clean:cli": "npm run clean --workspace=cli",
    "test": "npm run core:test",
    "test:core": "npm run test --workspace=core",
    "server": "npm run dev --workspace=cli"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
